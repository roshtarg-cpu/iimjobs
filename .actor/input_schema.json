{
  "title": "IIMJobs Scraper",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "jobTitle": {
      "title": "Job Title / Keywords",
      "type": "string",
      "description": "Job role or keyword to search e.g. Product Manager, Investment Banking, Strategy Consultant, CFO, Marketing Manager",
      "editor": "textfield"
    },
    "location": {
      "title": "Location",
      "type": "string",
      "description": "City to filter jobs e.g. Mumbai, Delhi, Bangalore, Hyderabad. Leave empty for all India.",
      "editor": "textfield"
    },
    "maxResults": {
      "title": "Max Results",
      "type": "integer",
      "description": "Maximum number of job listings to scrape",
      "default": 50,
      "editor": "number"
    },
    "proxyConfiguration": {
      "title": "Proxy Configuration",
      "type": "object",
      "description": "Use Apify residential proxies to avoid blocks.",
      "editor": "proxy",
      "prefill": {
        "useApifyProxy": true,
        "apifyProxyGroups": ["RESIDENTIAL"]
      }
    }
  },
  "required": ["jobTitle"]
}
